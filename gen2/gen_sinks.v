
// packet sink 0

packet_sink #(
	.ID(0),
	.PORT_BITS(PORT_BITS)
) sink_0 (
	clk,
	reset,
	snk_req[0],
	snk_data[0],
	snk_ack[0]
);

// packet sink 1

packet_sink #(
	.ID(1),
	.PORT_BITS(PORT_BITS)
) sink_1 (
	clk,
	reset,
	snk_req[1],
	snk_data[1],
	snk_ack[1]
);

// packet sink 2

packet_sink #(
	.ID(2),
	.PORT_BITS(PORT_BITS)
) sink_2 (
	clk,
	reset,
	snk_req[2],
	snk_data[2],
	snk_ack[2]
);

// packet sink 3

packet_sink #(
	.ID(3),
	.PORT_BITS(PORT_BITS)
) sink_3 (
	clk,
	reset,
	snk_req[3],
	snk_data[3],
	snk_ack[3]
);

// packet sink 4

packet_sink #(
	.ID(4),
	.PORT_BITS(PORT_BITS)
) sink_4 (
	clk,
	reset,
	snk_req[4],
	snk_data[4],
	snk_ack[4]
);

// packet sink 5

packet_sink #(
	.ID(5),
	.PORT_BITS(PORT_BITS)
) sink_5 (
	clk,
	reset,
	snk_req[5],
	snk_data[5],
	snk_ack[5]
);

// packet sink 6

packet_sink #(
	.ID(6),
	.PORT_BITS(PORT_BITS)
) sink_6 (
	clk,
	reset,
	snk_req[6],
	snk_data[6],
	snk_ack[6]
);

// packet sink 7

packet_sink #(
	.ID(7),
	.PORT_BITS(PORT_BITS)
) sink_7 (
	clk,
	reset,
	snk_req[7],
	snk_data[7],
	snk_ack[7]
);

// packet sink 8

packet_sink #(
	.ID(8),
	.PORT_BITS(PORT_BITS)
) sink_8 (
	clk,
	reset,
	snk_req[8],
	snk_data[8],
	snk_ack[8]
);

// packet sink 9

packet_sink #(
	.ID(9),
	.PORT_BITS(PORT_BITS)
) sink_9 (
	clk,
	reset,
	snk_req[9],
	snk_data[9],
	snk_ack[9]
);

// packet sink 10

packet_sink #(
	.ID(10),
	.PORT_BITS(PORT_BITS)
) sink_10 (
	clk,
	reset,
	snk_req[10],
	snk_data[10],
	snk_ack[10]
);

// packet sink 11

packet_sink #(
	.ID(11),
	.PORT_BITS(PORT_BITS)
) sink_11 (
	clk,
	reset,
	snk_req[11],
	snk_data[11],
	snk_ack[11]
);
